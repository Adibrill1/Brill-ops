# App icons — not yet supplied

`manifest.webmanifest` references `icon-192.png`, `icon-512.png` and
`icon-maskable-512.png`. **These files do not exist yet**, because no Brill Ops branding
came with the handoff.

Until they are added, the install prompt works but the installed app uses a browser
default icon. Export the three sizes from the real logo and drop them here — nothing else
needs changing.

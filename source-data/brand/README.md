# brand — empty in the handoff

`Assets/` and `Branding/` arrived containing nothing but macOS `.DS_Store` files, and
`Assets/September 2026/` was an empty subfolder.

No Stars for Peace branding exists yet. This is why the Stars for Peace campaign is
seeded as a `draft` with `confidence = 'estimated'` and a `config.todo` list, rather than
as an active campaign with invented copy. See
[assumptions §A1](../../docs/import/assumptions-and-inferred-data.md#a1--the-campaign-was-created-as-a-draft-with-placeholder-values).

Drop real brand assets here, then upload them to the `campaign-media` Storage bucket and
set `campaigns.hero_image_url`.
